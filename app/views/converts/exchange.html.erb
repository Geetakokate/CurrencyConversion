<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
    </style>
  </head>

  <body>
    <%= form_for(:converts, url: converts_path, remote: true) do |f| %>
      <%= f.select :currency_code, CURRENCE_CODES, {}, {} %>
      <%= f.text_field :amount %>
      <%= f.select :currency_code_to_convert, CURRENCE_CODES, {}, {} %>
      <%= f.submit 'Convert', class: { id: 'convet'}%>
    <% end %>
  </body>

</html>
